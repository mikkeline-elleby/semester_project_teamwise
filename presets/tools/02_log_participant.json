{
  "type": "function",
  "function": {
    "name": "log_participant",
    "description": "Store a participant's name and role for later use in the session.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Participant's preferred name."
        },
        "role": {
          "type": "string",
          "description": "Participant's role in the project or course."
        }
      },
      "required": ["name", "role"]
    }
  }
}
